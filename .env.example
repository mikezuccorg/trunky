# This file is for reference only
# The app does not use .env files - API keys are entered in the UI
# and stored in browser localStorage

# If you want to set a default API key for development,
# you can modify the code to read from environment variables
# ANTHROPIC_API_KEY=your_api_key_here
